<UserControl x:Class="TwitchStressToolkit.UI.Views.BotManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="16" Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Bot Manager"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding AccountsFile, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="White"
                     Background="#3D3D3D"
                     Margin="0,0,8,0"
                     VerticalContentAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="Load File"
                    Command="{Binding LoadAccountsCommand}"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="White"
                     Background="#3D3D3D"
                     Margin="0,0,8,0"
                     VerticalContentAlignment="Center"/>

            <TextBox Grid.Column="1"
                     Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="White"
                     Background="#3D3D3D"
                     Margin="0,0,8,0"
                     VerticalContentAlignment="Center"/>

            <TextBox Grid.Column="2"
                     Text="{Binding NewAuthToken, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="White"
                     Background="#3D3D3D"
                     Margin="0,0,8,0"
                     VerticalContentAlignment="Center"/>

            <TextBox Grid.Column="3"
                     Text="{Binding NewChannel, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="White"
                     Background="#3D3D3D"
                     Margin="0,0,8,0"
                     VerticalContentAlignment="Center"/>

            <Button Grid.Column="4"
                    Content="Add"
                    Command="{Binding AddAccountCommand}"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="Remove" Command="{Binding RemoveAccountCommand}" Margin="0,0,8,0"/>
            <Button Content="Change Channel" Command="{Binding ChangeChannelCommand}" Margin="0,0,8,0"/>
            <Button Content="Validate" Command="{Binding ValidateAccountCommand}" Margin="0,0,8,0"/>
            <Button Content="Validate All" Command="{Binding ValidateAllCommand}" Margin="0,0,8,0"/>
        </StackPanel>

        <DataGrid Grid.Row="4"
                  ItemsSource="{Binding Accounts}"
                  SelectedItem="{Binding SelectedAccount}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Background="#1E1E1E"
                  Foreground="White"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="60" Binding="{Binding Id, StringFormat={}{0:N}}"/>
                <DataGridTextColumn Header="Username" Width="150" Binding="{Binding Username}"/>
                <DataGridTextColumn Header="Channel" Width="150" Binding="{Binding CurrentChannel}"/>
                <DataGridTextColumn Header="Status" Width="100" Binding="{Binding Status}"/>
                <DataGridTextColumn Header="Bans" Width="60" Binding="{Binding BanCount}"/>
                <DataGridTextColumn Header="Last Used" Width="150" Binding="{Binding LastUsedAt, StringFormat={}{0:g}}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="5" Background="#2D2D2D" Foreground="#AAAAAA">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding Accounts.Count, StringFormat='Total: {0}'}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
