<UserControl x:Class="TwitchStressToolkit.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">

    <Grid Margin="16" Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Dashboard"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextBrush}"/>
            <TextBlock Text="Live health snapshot for the current session."
                       Margin="0,8,0,0"
                       Foreground="{DynamicResource SubtextBrush}"/>
        </StackPanel>

        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <Border Style="{StaticResource SummaryCardStyle}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Active" Style="{StaticResource SummaryLabelStyle}"/>
                    <TextBlock Text="{Binding ActiveConnections}" Style="{StaticResource SummaryValueStyle}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SummaryCardStyle}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Msg/sec" Style="{StaticResource SummaryLabelStyle}"/>
                    <TextBlock Text="{Binding MessagesPerSecond}" Style="{StaticResource SummaryValueStyle}" Foreground="{DynamicResource SuccessBrush}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SummaryCardStyle}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Latency" Style="{StaticResource SummaryLabelStyle}"/>
                    <TextBlock Text="{Binding Latency, StringFormat={}{0:F0} ms}" Style="{StaticResource SummaryValueStyle}" Foreground="{DynamicResource AccentBrush}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource SummaryCardStyle}">
                <StackPanel>
                    <TextBlock Text="Errors" Style="{StaticResource SummaryLabelStyle}"/>
                    <TextBlock Text="{Binding ErrorCount}" Style="{StaticResource SummaryValueStyle}" Foreground="{DynamicResource ErrorBrush}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" Padding="12" CornerRadius="18" Margin="0,0,0,10">
                <lvc:CartesianChart Series="{Binding LatencyChartSeries}"
                                    XAxes="{Binding XAxes}"
                                    YAxes="{Binding YAxes}"
                                    TooltipPosition="Top"
                                    AnimationsSpeed="00:00:00.150"/>
            </Border>

            <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" Padding="12" CornerRadius="18">
                <lvc:CartesianChart Series="{Binding ThroughputChartSeries}"
                                    XAxes="{Binding XAxes}"
                                    YAxes="{Binding YAxes}"
                                    AnimationsSpeed="00:00:00.150"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
